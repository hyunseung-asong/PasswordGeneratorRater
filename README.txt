1. Install node.js and npm
2. npm init
3. npm install browserify
4. npm install -g browserify
5. npm install random-words
6. each time js file is updated, have to "compile" js file, broswerify main.js -o bundle.js